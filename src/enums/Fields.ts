export enum FieldTypes {
  INPUT = 'input',
  PASSWORD = 'password',
  EMAIL = 'email',
  URL = 'url',
  TEL = 'tel',
  TEXTAREA = 'textarea',

  DATE_TIME = 'date_time',

  BUTTON = 'button',

  CHECKBOX = 'checkbox',
  RADIO = 'radio',

  SELECT = 'select',

  FILE = 'file',

  SEARCH = 'search',
}

export enum FieldViewTypes {
  READONLY = 'readonly',
  DISABLED = 'disabled',
  EDIT = 'EDIT',
}
