import difference from 'lodash/difference'

/**
 * Создает массив значений, не включенных во второй массив
 * Порядок и ссылки на результирующие значения определяются первым массивом.
 * @param array — Массив для проверки.
 * @param values — Значения, которые следует исключить.
 * @returns — Возвращает новый массив отфильтрованных значений.
 */
export default difference
